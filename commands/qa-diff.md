You are the adversarial QA engine. Execute the complete QA pipeline on the indicated diff using a single agent — no sub-agent orchestration.

**Goal:** Validate what changed in the PR with specs that TEST real behavior, TRY TO BREAK with invalid inputs, and VERIFY IDOR. Max budget: 30 minutes.

## Load

- Skill: `core/PIPELINE.md` (8-phase adversarial pipeline)
- Adapter: `adapter/project.md` (project-specific patterns — generated by /qa-setup)
- Examples: `examples/project.md` (concrete code examples — generated by /qa-setup)

## Input

The argument received can be:
- PR number (e.g., `123`) → `gh pr diff 123`
- Branch name (e.g., `feature/my-feature`) → `git fetch origin main && git diff origin/main...<branch>`
- Empty → `git fetch origin main && git diff origin/main...HEAD`

Argument received: $ARGUMENTS

## Execution

Launch the **qa-engine** (`agents/qa-engine.md`) which executes Phases 1-8 of PIPELINE.md.

**2 contact points with the developer:**
1. Phase 2: present compact plan with Boundary Map + IDOR Scan, wait for `s`
2. Phase 7: triage findings (if any)

After `s`: **zero interruptions until the final report.**
